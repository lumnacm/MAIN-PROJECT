<main id="main" class="main">

    <div class="pagetitle">
        <h1>Manage Staffs</h1>
        <nav>
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/admin">Dashboard</a></li>
                <li class="breadcrumb-item">Staffs</li>
                <li class="breadcrumb-item active">All-Staffs</li>
            </ol>
        </nav>
    </div><!-- End Page Title -->

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="card-title">All Staffs</h5>
                            <a href="/admin/staffs/add-staff" class="btn btn-dark">+ Add New Staff</a>
                        </div>

                        <div class="table-responsive">
                            <table class="table datatable">
                                <thead>
                                    <tr>
                                        <th>No.</th>
                                        <th>Staff Name</th>
                                        <th>Dept</th>
                                        <th>Email</th>
                                        <th>Phone</th>
                                        <th>Address</th>
                                        <th>Credentials</th>
                                        <th>Manage</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each staffs}}
                                    <tr>
                                        <td>{{incremented @index}}</td>
                                        <td>{{this.staffname}}</td>
                                        <td>{{this.dept}}</td>
                                        <td>{{this.email}}</td>
                                        <td>{{this.phone}}</td>
                                        <td>{{this.address}}</td>
                                        <td>
                                            <p>Username : {{this.username}}</p>
                                            <p>Password : {{this.password}}</p>
                                        </td>
                                        <td>
                                            <div class="d-flex gap-2">


                                                <form action="/admin/staffs/delete-staff/{{this._id}}" method="POST">
                                                    <button type="submit"
                                                        onclick="return confirm('Are You Sure to Delete This Staff ?')"
                                                        class="btn btn-danger btn-sm"><i
                                                            class="bi bi-trash"></i></button>
                                                </form>
                                            </div>
                                        </td>

                                    </tr>
                                    {{/each}}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>


            </div>
        </div>
    </section>

</main><!-- End #main -->